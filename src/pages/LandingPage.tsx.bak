import React, { useState } from 'react';
import { Navbar } from '../components/investment/Navbar';
import { Button } from '../components/ui/Button';
import {
  Play,
  Users,
  BarChart3,
  Globe,
  Star,
  Quote,
  TrendingUp,
  Wallet,
  ShieldCheck,
  Zap,
  Smartphone,
  Check,
  ChevronDown,
  Twitter,
  Linkedin,
  Youtube,
  Instagram,
} from 'lucide-react';

interface LandingPageProps {
  onNavigate: (page: string) => void;
}

type Testimonial = {
  name: string;
  role: string;
  company: string;
  image: string;
  quote: string;
  rating: number;
};

export function LandingPage({ onNavigate }: LandingPageProps) {
  const [infoModalOpen, setInfoModalOpen] = useState(false);
  const [infoKey, setInfoKey] = useState<string | null>(null);
  const [openFaq, setOpenFaq] = useState<number | null>(null);

  const testimonials: Testimonial[] = [
    {
      name: 'Sarah Chen',
      role: 'Portfolio Manager',
      company: 'Apex Capital',
      image:
        'https://images.unsplash.com/photo-1494790108377-be9c29b29330?w=100&h=100&fit=crop&crop=face',
      quote:
        'StockFx has transformed how I manage client portfolios. The real-time analytics and execution speed are unmatched in the industry.',
      rating: 5,
    },
    {
      name: 'Michael Torres',
      role: 'Day Trader',
      company: 'Independent',
      image:
        'https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=100&h=100&fit=crop&crop=face',
      quote:
        'I switched from three different platforms to StockFx. The interface is intuitive, fees are transparent, and support is always there when I need it.',
      rating: 5,
    },
    {
      name: 'Emily Watson',
      role: 'Retirement Planner',
      company: 'Watson Financial',
      image:
        'https://images.unsplash.com/photo-1438761681033-6461ffad8d80?w=100&h=100&fit=crop&crop=face',
      quote:
        'My clients love the mobile app. They can check their investments anytime, and the automated rebalancing saves me hours every week.',
      rating: 5,
    },
    {
      name: 'David Park',
      role: 'Tech Entrepreneur',
      company: 'StartupX',
      image:
        'https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=100&h=100&fit=crop&crop=face',
      quote:
        'As someone who values both security and speed, StockFx delivers on both. Their API integration with my fintech tools is seamless.',
      rating: 5,
    },
  ];

  const faqs = [
    {
      question: 'How do I get started with StockFx Investment?',
      answer:
        'Getting started is easy! Simply create a free account, complete our quick verification process, and fund your account. You can start trading within minutes. We also offer a demo account with $100,000 in virtual funds to practice.',
    },
    {
      question: 'What are the fees and commissions?',
      answer:
        'We believe in transparent pricing. Stock and ETF trades are commission-free. Crypto trades have a 0.5% fee. Options contracts are $0.65 per contract. There are no account minimums, no maintenance fees, and no hidden charges.',
    },
    {
      question: 'Is my money safe with StockFx?',
      answer:
        'Absolutely. Your cash is FDIC insured up to $250,000, and securities are protected by SIPC up to $500,000. We use bank-level 256-bit encryption, two-factor authentication, and store 95% of assets in cold storage.',
    },
  ];

  const pricingPlans = [
    {
      name: 'Starter',
      price: 'Free',
      period: 'forever',
      description: 'Perfect for beginners',
      features: ['Commission-free stock trades', 'Basic charting tools', 'Mobile app access', 'Email support', '2 watchlists'],
      cta: 'Get Started',
      popular: false,
    },
    {
      name: 'Pro',
      price: '$9.99',
      period: '/month',
      description: 'For active traders',
      features: ['Everything in Starter', 'Advanced charting & indicators', 'Real-time Level 2 data', 'Priority support', 'Unlimited watchlists', 'API access', 'Tax optimization tools'],
      cta: 'Start Free Trial',
      popular: true,
    },
    {
      name: 'Enterprise',
      price: 'Custom',
      period: '',
      description: 'For institutions',
      features: ['Everything in Pro', 'Dedicated account manager', 'Custom integrations', 'White-label solutions', 'SLA guarantees', 'Compliance tools', 'Bulk trading discounts'],
      cta: 'Contact Sales',
      popular: false,
    },
  ];

  const openInfo = (key: string) => {
    setInfoKey(key);
    setInfoModalOpen(true);
  };
  const closeInfo = () => {
    setInfoKey(null);
    setInfoModalOpen(false);
  };

  const infoContent: Record<string, JSX.Element> = {
    pricing: (
      <div className="space-y-4">
        <h3 className="text-xl font-bold">Pricing Plans</h3>
        <p className="text-sm text-slate-600">Choose a plan that fits your trading style. Start free and upgrade when you need advanced features.</p>
        <div className="grid md:grid-cols-3 gap-4">
          {pricingPlans.map((plan) => (
            <div key={plan.name} className="bg-slate-50 p-4 rounded-lg border border-slate-100">
              <div className="flex items-baseline justify-between">
                <div>
                  <h4 className="font-semibold text-slate-900">{plan.name}</h4>
                  <div className="text-slate-500 text-sm">{plan.description}</div>
                </div>
                <div className="text-right">
                  <div className="font-black text-lg text-slate-900">{plan.price}</div>
                  <div className="text-sm text-slate-500">{plan.period}</div>
                </div>
              </div>
              <ul className="mt-3 text-sm text-slate-600 space-y-1">{plan.features.map((f, i) => <li key={i}>• {f}</li>)}</ul>
            </div>
          ))}
        </div>
      </div>
    ),
    security: (
      <div className="space-y-4">
        <h3 className="text-xl font-bold">Bank-grade Security</h3>
        <p className="text-sm text-slate-600">We build security into every layer: 256-bit encryption in transit and at rest, strict key management, role-based access controls, two-factor authentication, SOC 2 Type II audits, FDIC insurance on cash and SIPC protection for securities, regular penetration testing, and a public bug bounty program.</p>
      </div>
    ),
    api: (
      <div className="space-y-4">
        <h3 className="text-xl font-bold">Public API & Integrations</h3>
        <p className="text-sm text-slate-600">Programmatic access to market data, orders, and account information — built for developers and teams. Features include real-time market data, order placement, webhooks & streaming, and SDKs for JavaScript, Python, and Go.</p>
        <pre className="bg-slate-900 text-white p-3 rounded text-xs overflow-auto">curl -X GET "https://api.stockfx.example/v1/markets/AAPL" -H "Authorization: Bearer YOUR_API_KEY"</pre>
      </div>
    ),
    markets: (
      <div className="space-y-4">
        <h3 className="text-xl font-bold">Market Data</h3>
        <p className="text-sm text-slate-600">Real-time quotes, historical bars, and streaming market feeds. Use REST endpoints for snapshots and historical data, WebSockets for high-frequency ticks, and webhooks for account events and fills. Contact <a href="mailto:api@stockfx.example" className="text-amber-500 hover:underline">api@stockfx.example</a> for API access and onboarding.</p>
      </div>
    ),
    about: (
      <div className="space-y-4">
        <h3 className="text-xl font-bold">About StockFx</h3>
        <p className="text-sm text-slate-600">StockFx is on a mission to make investing accessible and powerful for everyone. Founded by traders and engineers, we focus on speed, transparency, and security — delivering professional tools with zero complexity.</p>
      </div>
    ),
    contact: (
      <div className="space-y-4">
        <h3 className="text-xl font-bold">Contact</h3>
        <p className="text-sm text-slate-600">For sales: <a href="mailto:sales@stockfx.example" className="text-amber-500 hover:underline">sales@stockfx.example</a></p>
        <p className="text-sm text-slate-600">Support: <a href="mailto:support@stockfx.example" className="text-amber-500 hover:underline">support@stockfx.example</a></p>
      </div>
    ),
    privacy: (
      <div className="space-y-4">
        <h3 className="text-xl font-bold">Privacy Policy</h3>
        <p className="text-sm text-slate-600">Effective date: January 1, 2026</p>
        <p className="text-sm text-slate-600">We respect your privacy and are committed to protecting your personal information...</p>
      </div>
    ),
  };

  return (
    <div className="min-h-screen bg-slate-50 font-sans">
      <Navbar onNavigate={onNavigate} />

      {/* Hero */}
      <section className="pt-32 pb-20 lg:pt-44 lg:pb-32 relative bg-gradient-to-b from-slate-900 to-slate-800">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center relative z-10">
          <div className="inline-flex items-center px-4 py-2 rounded-full bg-amber-500/10 border border-amber-500/20 text-amber-400 text-sm font-medium mb-8">
            <span className="flex h-2 w-2 rounded-full bg-amber-400 mr-2 animate-pulse" />
            🎉 New: Zero-fee trading on your first $10,000
          </div>
          <h1 className="text-5xl md:text-7xl font-black text-white tracking-tight mb-8 leading-[0.9]">
            Invest Smarter.
            <br />
            <span className="bg-gradient-to-r from-amber-400 via-amber-500 to-orange-500 bg-clip-text text-transparent">Grow Faster.</span>
          </h1>
          <p className="text-xl md:text-2xl text-slate-300 mb-12 max-w-2xl mx-auto">The next-generation platform for stocks, crypto, and ETFs. Professional tools. Zero complexity.</p>

          <div className="flex flex-col sm:flex-row items-center justify-center gap-4 mb-8">
            <Button size="lg" className="w-full sm:w-auto px-10 py-4 text-lg bg-gradient-to-r from-amber-500 to-orange-500" onClick={() => onNavigate('register')}>Start Investing Free</Button>
            <Button variant="outline" size="lg" className="w-full sm:w-auto px-10 py-4 text-lg border-slate-600 text-white" leftIcon={<Play className="h-5 w-5" />}>Watch Demo</Button>
          </div>
        </div>
      </section>

      {/* Testimonials */}
      <section className="py-24 bg-slate-900 relative overflow-hidden">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
          <div className="text-center max-w-3xl mx-auto mb-16">
            <h2 className="text-4xl md:text-5xl font-bold text-white mb-4">Loved by <span className="bg-gradient-to-r from-amber-400 to-orange-500 bg-clip-text text-transparent">traders worldwide</span></h2>
            <p className="text-xl text-slate-400">Join over 2 million investors who trust StockFx</p>
          </div>

          <div className="grid md:grid-cols-2 gap-6 mb-8">
            {testimonials.slice(0, 2).map((t, i) => (
              <div key={i} className="bg-white/5 backdrop-blur-sm border border-white/10 rounded-3xl p-8 hover:bg-white/10 transition-colors">
                <div className="flex items-center gap-1 mb-6">
                  {[...Array(t.rating)].map((_, j) => <Star key={j} className="h-5 w-5 fill-amber-400 text-amber-400" />)}
                </div>
                <Quote className="h-10 w-10 text-amber-500/30 mb-4" />
                <p className="text-lg text-slate-300 mb-8 leading-relaxed">"{t.quote}"</p>
                <div className="flex items-center">
                  <img src={t.image} alt={t.name} className="h-12 w-12 rounded-full object-cover border-2 border-amber-500/30" />
                  <div className="ml-4">
                    <div className="font-semibold text-white">{t.name}</div>
                    <div className="text-sm text-slate-400">{t.role} at {t.company}</div>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* FAQ */}
      <section className="py-24 bg-slate-50">
        <div className="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center mb-16">
            <h2 className="text-4xl md:text-5xl font-bold text-slate-900 mb-4">Frequently asked <span className="bg-gradient-to-r from-amber-500 to-orange-500 bg-clip-text text-transparent">questions</span></h2>
            <p className="text-xl text-slate-600">Everything you need to know about StockFx</p>
          </div>

          <div className="space-y-4">
            {faqs.map((faq, i) => (
              <div key={i} className="bg-white rounded-2xl border border-slate-200 overflow-hidden">
                <button className="w-full px-6 py-5 text-left flex items-center justify-between" onClick={() => setOpenFaq(openFaq === i ? null : i)}>
                  <span className="font-semibold text-slate-900 pr-4">{faq.question}</span>
                  <ChevronDown className={`h-5 w-5 text-slate-400 transition-transform ${openFaq === i ? 'rotate-180' : ''}`} />
                </button>
                <div className={`overflow-hidden transition-all duration-300 ${openFaq === i ? 'max-h-96' : 'max-h-0'}`}>
                  <p className="px-6 pb-5 text-slate-600 leading-relaxed">{faq.answer}</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Final CTA & Footer */}
      <section className="py-24 bg-gradient-to-br from-slate-900 to-slate-800 relative overflow-hidden">
        <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
          <h2 className="text-4xl md:text-6xl font-black text-white mb-6">Ready to start your <br /><span className="bg-gradient-to-r from-amber-400 to-orange-500 bg-clip-text text-transparent">investment journey?</span></h2>
          <p className="text-xl text-slate-300 mb-10 max-w-2xl mx-auto">Join over 2 million investors who trust StockFx. Create your free account today and get $50 in free stock.</p>
          <div className="flex flex-col sm:flex-row items-center justify-center gap-4">
            <Button size="lg" className="px-12 py-4 text-lg bg-gradient-to-r from-amber-500 to-orange-500" onClick={() => onNavigate('register')}>Create Free Account</Button>
            <Button variant="outline" size="lg" className="px-12 py-4 text-lg border-slate-600 text-white" onClick={() => onNavigate('login')}>Sign In</Button>
          </div>
        </div>
      </section>

      <footer className="bg-slate-900 border-t border-slate-800 pt-20 pb-10">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-white">
          <div className="grid grid-cols-2 md:grid-cols-5 gap-8 mb-16">
            <div className="col-span-2">
              <div className="flex items-center mb-6">
                <div className="h-10 w-10 bg-gradient-to-br from-amber-400 to-orange-500 rounded-xl flex items-center justify-center mr-3"><TrendingUp className="h-6 w-6 text-white" /></div>
                <div>
                  <span className="text-xl font-black">Stock<span className="text-amber-400">Fx</span></span>
                  <span className="block text-[10px] uppercase tracking-widest text-slate-400">Investment</span>
                </div>
              </div>
              <p className="text-slate-400 text-sm mb-6 max-w-xs">The next-generation investment platform for stocks, crypto, and ETFs. Trade smarter, grow faster.</p>
              <div className="flex space-x-4">
                {[Twitter, Linkedin, Youtube, Instagram].map((Icon, i) => (
                  <a key={i} href="#" className="h-10 w-10 rounded-full bg-slate-800 flex items-center justify-center text-slate-400 hover:bg-amber-500 hover:text-white transition-colors"><Icon className="h-5 w-5" /></a>
                ))}
              </div>
            </div>
            <div>
              <h4 className="font-bold text-white mb-4">Platform</h4>
              <ul className="space-y-3 text-sm text-slate-400">
                <li><a href="#features" className="hover:text-amber-400">Features</a></li>
                <li><a href="#" onClick={(e) => { e.preventDefault(); openInfo('pricing'); }} className="hover:text-amber-400">Pricing</a></li>
                <li><a href="#" onClick={(e) => { e.preventDefault(); openInfo('security'); }} className="hover:text-amber-400">Security</a></li>
                <li><a href="#" onClick={(e) => { e.preventDefault(); openInfo('api'); }} className="hover:text-amber-400">API</a></li>
              </ul>
            </div>
            <div>
              <h4 className="font-bold text-white mb-4">Company</h4>
              <ul className="space-y-3 text-sm text-slate-400">
                <li><a href="#" onClick={(e) => { e.preventDefault(); onNavigate('login'); }} className="hover:text-amber-400">Login</a></li>
                <li><a href="#" onClick={(e) => { e.preventDefault(); onNavigate('about'); }} className="hover:text-amber-400">About Us</a></li>
                <li><a href="#" onClick={(e) => { e.preventDefault(); openInfo('contact'); }} className="hover:text-amber-400">Contact</a></li>
              </ul>
            </div>
            <div>
              <h4 className="font-bold text-white mb-4">Legal</h4>
              <ul className="space-y-3 text-sm text-slate-400">
                <li><a href="/policies/privacy.html" target="_blank" rel="noopener noreferrer" className="hover:text-amber-400">Privacy Policy</a></li>
                <li><a href="/policies/terms.html" target="_blank" rel="noopener noreferrer" className="hover:text-amber-400">Terms of Service</a></li>
                <li><a href="/policies/disclosures.html" target="_blank" rel="noopener noreferrer" className="hover:text-amber-400">Disclosures</a></li>
                <li><a href="/policies/cookies.html" target="_blank" rel="noopener noreferrer" className="hover:text-amber-400">Cookie Policy</a></li>
              </ul>
            </div>
          </div>

          <div className="border-t border-slate-800 pt-8 flex flex-col md:flex-row justify-between items-center">
            <p className="text-sm text-slate-500">© 2024 StockFx Investment Inc. All rights reserved. NMLS #123456</p>
            <p className="text-xs text-slate-600 mt-4 md:mt-0 max-w-md text-center md:text-right">Securities offered through StockFx Securities LLC, member FINRA/SIPC. Cryptocurrency trading provided by StockFx Crypto LLC.</p>
          </div>
        </div>
      </footer>
    </div>
  );
}
